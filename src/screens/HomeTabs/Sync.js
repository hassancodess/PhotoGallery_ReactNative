import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Sync = () => {
  return (
    <View>
      <Text>Sync</Text>
    </View>
  );
};

export default Sync;

const styles = StyleSheet.create({});
